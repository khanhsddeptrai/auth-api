import { Router } from 'express';
const router = Router();
// import { createUser, getListUser } from '../controllers/userController';

// router.post('/create', createUser);
// router.get('/get-all', getListUser);

export default router;